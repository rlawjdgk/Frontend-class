// 오후 문제
// 숫자 a와 b를 받고
// alert로 두수를 비교하여 a가 b보다 큽니다를 출력해주세요

// 삼항 연산자로 풀기!!!!!!!!!!!!!
// 조건 ? true일때 행위 : false일때 행위;

// const a = Number(prompt("숫자를 입력하세요"));
// const b = Number(prompt("숫자를 입력하세요"));

// let result = a > b;

// result = result ? `a가 b보다 큽니다!` : `a가 b보다 작습니다!`;

// alert(`${result}`);

const a = Number(prompt("숫자를 입력하세요"));
const b = Number(prompt("숫자를 입력하세요"));

let result = a > b;

result = result ? `a가 b보다 큽니다` : `a가 b보다 작습니다`;

alert(`${result}`);
